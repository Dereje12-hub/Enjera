import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function RegisterScene( {navigation} ) {
  return (
    <View>
      <Text>RegisterScene</Text>
    </View>
  )
}

const styles = StyleSheet.create({})